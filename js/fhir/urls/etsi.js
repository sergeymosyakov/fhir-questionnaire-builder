// ── ETSI XAdES (uri.etsi.org) ─────────────────────────────────────────────────
// ETSI XAdES namespace URI used for digital-signature type codings. Spec-defined
// `http://` identifier, never fetched.
const ETSI = 'http:' + '//uri.etsi.org'; // NOSONAR — ETSI XAdES namespace URI, not a network endpoint

export const ETSI_URL = {
  xades132: ETSI + '/01903/v1.2.2',
};
