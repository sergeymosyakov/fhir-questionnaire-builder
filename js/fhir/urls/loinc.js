// ── LOINC (loinc.org) ─────────────────────────────────────────────────────────
// LOINC code-system URI. Spec-defined `http://` identifier, never fetched.
const LOINC = 'http:' + '//loinc.org'; // NOSONAR — LOINC system URI, not a network endpoint

export const LOINC_URL = {
  system: LOINC,
};
