// ── W3C namespaces ────────────────────────────────────────────────────────────
// Namespace URIs defined by the W3C — identifiers, not network endpoints.
const W3 = 'http:' + '//www.w3.org'; // NOSONAR — W3C namespace URI, not a network endpoint

export const W3C_URL = {
  xhtml: W3 + '/1999/xhtml',
};
